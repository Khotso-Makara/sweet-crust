# Sweet Crust - Frontend

## Setup

1. Install dependencies
   ```
   cd frontend
   npm install
   ```

2. Run dev server
   ```
   npm run dev
   ```

The frontend expects the backend to run at http://localhost:5000